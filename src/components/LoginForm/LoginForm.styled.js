import styled from 'styled-components';

export const FormContainer = styled.div`
  background: var(--white);
  padding: 20px;
  border-radius: 5px;
  margin-bottom: 30px;
  max-width: 50%;
  margin-left: auto;
  margin-right: auto;
`;